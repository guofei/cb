<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.name %></td>
    <td><%= link_to 'Show', [:admins, category] %></td>
    <td><%= link_to 'Edit', edit_admins_category_path(category) %></td>
    <td><%= link_to 'Destroy', [:admins, category], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_admins_category_path %>
