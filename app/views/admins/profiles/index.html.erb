<table>
  <tr>
    <th>Name</th>
    <th>Nickname</th>
    <th>Tel</th>
    <th>Mail</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @profiles.each do |profile| %>
  <tr>
    <td><%= profile.name %></td>
    <td><%= profile.nickname %></td>
    <td><%= profile.tel %></td>
    <td><%= profile.mail %></td>
    <td><%= link_to 'Show', [:admins, profile] %></td>
    <td><%= link_to 'Edit', edit_admins_profile_path(profile) %></td>
    <td><%= link_to 'Destroy', [:admins, profile], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Profile', new_admins_profile_path %>
