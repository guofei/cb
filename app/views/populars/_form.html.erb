<%= form_for(@popular) do |f| %>
  <% if @popular.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@popular.errors.count, "error") %> prohibited this popular from being saved:</h2>

      <ul>
      <% @popular.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :commodity %><br />
    <%= f.number_field :commodity_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
