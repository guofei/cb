<h2>these people want to buy</h2>
<p>
<% @commodity.orders.each do |order| %>
  <% order.state = true %>
  <tr>
    <th><%= order.user.email %></th>
    <th><div id="state"><%= render 'orders/dealform', :order => order %></div></th>
    <br />
  </tr>
<% end %>
</p>
