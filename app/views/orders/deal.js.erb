$(function($){
      $('#dealorder')
          .bind("ajax:complete", function(){
                    $('#mail').html(
                        "<%= @order.user.profile.mail %>"
                    );
                    $('#tel').html(
                        "<%= @order.user.profile.tel %>"
                    );
                    $('#state').html(
                        "OK!"
                    );
                });
  });
